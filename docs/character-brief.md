RM 행성 대표 캐릭터 제작 — 지시서
=====================================

> 2026-08-24 작성 · 2026-08-25 갱신. Phase 7-5(홈 보상 오브젝트 적용)를 마친 뒤
> 갈라져 나온 회차다. 캐릭터를 다시 뽑을 때 이 문서를 먼저 읽는다.

## 지금 상태 (2026-08-25)

**16학기 전부 캐릭터가 들어가 있다.** Try 2의 결과물을 webp로 변환해 32화면
(수학·과학 × 16학기)에 설치했다. `nav/home.html`의 `HAS_CHAR`는 전 학기를 돌고,
`CHAR_V`는 3이다. **교체될 수 있는 상태**이고, 교체하면 `CHAR_V`를 올린다.

원본 PNG 16장(1254px 정사각 · 누끼 완료)은 여기 있다:
`~/Documents/_source/RM/generated/img/character/planet-<학기키>-<슬러그>-v*.png`

### 남은 판단거리

- **`elem-3-1` 로봇만 규격이 다르다.** 혼자 콩알눈·2등신이고 나머지 15종은 큰 눈에
  3등신에 가깝다. 라인업으로 보면 첫 칸이 튄다. 새 문법으로 다시 뽑을지 미정.
- **묻힘이 걱정되는 학기**: `elem-6-2`(크림 몸 · 베이지 배경), `high-1-1`(베이지 ·
  모래 배경). 지금은 미완료 오브젝트가 무채색 홀로그램이라 살지만, 학습이 진행돼
  오브젝트가 컬러로 바뀌면 다시 본다. **다시 뽑기 전에 드롭섀도를 세게 하는 쪽을
  먼저 검토할 것** — 훨씬 싸다.
- Try 2 결과에 사람형(파라오·아테나·마법사·요정·락커)이 많이 나왔다. 아래 그림 조건의
  「사람으로 표현하지 않는다」와 어긋나지만 결과물은 채택됐다. 다음 회차에서 이 조항을
  유지할지 정한다.

## 먼저 할 것

0. 아래 **「역할 분담」** 절. 그림 생성은 Codex가 하고 클로드 방은 하지 않는다.
1. 프로젝트 `CLAUDE.md`의 **⑤ 행성 홈** 절과 **tools/ 정리 규칙** 절.
2. `tools/planet-ref.py`와 `tools/char-on-bg.py`의 파일 머리 주석. 이 회차의 손발이다.
3. 기준 이미지(위 경로)를 실제로 열어 본다.

## 확정된 그림 조건 — 생성 모델에게 그대로 주는 문장

이 문장이 A행성 캐릭터를 만들어 낸 프롬프트다. **고치지 말고 그대로 쓴다.**
행성 설명(맨 아래 「이번에 만들 것」)만 그 학기의 것으로 바꾼다.

    ■ 세계관
    학생은 우주를 여행하며 학기마다 하나의 행성에 도착합니다.
    한 행성에는 대표 캐릭터가 딱 한 명 있고, 그 행성의 안내자이자 짝꿍입니다.
    같은 행성을 수학에서는 표면에서, 과학에서는 우주선 안에서 봅니다.
    그래서 한 캐릭터가 두 화면에 똑같이 서야 합니다.

    ■ 그림체 (첨부 오브젝트와 반드시 일치)
    · 플랫한 벡터 일러스트. 검은 외곽선을 두르지 않습니다.
    · 면과 면은 색의 밝기 차이로 구분합니다. 부드러운 그라데이션과 큰 면 위주.
    · 모서리가 둥글고 통통한 형태. 잔선과 잔디테일을 넣지 않습니다.
    · 위쪽에서 빛이 오고, 가장자리에 옅은 반사광이 있습니다.
    · 질감(붓결, 노이즈, 종이결)을 넣지 않습니다. 3D 렌더링 느낌도 아닙니다.

    ■ 비율과 얼굴 (2026-08-24 v2 개정 — 고정 템플릿이 아니라 범위다)
    · 비례는 자유. 다만 머리가 상대적으로 큰 귀여운 SD 느낌의 범위 안에서.
    · 얼굴 생김새도 자유. 기준 로봇과 눈·입을 맞출 필요 없습니다.
      단 심플하게 — 잔선 많은 눈, 사실적 이목구비, 무섭거나 징그러운 표현 금지.
    · 손과 발은 뭉툭한 덩어리로. 손가락을 나누지 않습니다.
    · 표정은 기본 웃는 얼굴 하나.

    ■ 자세와 구도
    · 정면을 보고 똑바로 서 있는 기본 자세. 팔은 몸에서 살짝 벌립니다(A자).
    · 크게 움직이는 포즈, 점프, 옆모습, 뒷모습은 만들지 않습니다.
    · 소품을 들어도 몸 밖으로 크게 뻗지 않게 합니다.
    · 나중에 몸·팔·다리를 따로 떼어 움직이게 만들 계획입니다. 어깨와 다리가
      몸통에 뭉개지지 않고 경계가 보이게 그려 주세요.
    · 발이 잘리지 않게 아래쪽에 여백을 둡니다. 바닥 그림자는 그리지 않습니다.

    ■ 구조 — 이것을 어기면 못 씁니다
    · 망토, 로브, 후드, 긴 옷으로 몸을 덮지 마세요. 팔다리와 몸통의 경계가
      사라지면 덩어리로 보이고, 나중에 부위를 떼어 움직일 수도 없습니다.
    · 머리 · 몸통 · 팔 · 다리가 눈으로 하나하나 구분되어야 합니다. 관절을 표시하세요.
    · 머리는 둥근 형태 하나로 유지하세요. 헬멧이나 투구로 얼굴을 파묻지 마세요.
      (예외: 심해 머구리처럼 헬멧이 존재의 정체성인 경우 — 둥근 창 안에
       얼굴이 크게 보이면 됩니다.)

    ■ 색 — 이것도 어기면 못 씁니다
    · 색은 두 가지 + 아주 작은 액센트. 세지 않아도 몇 색인지 알 수 있어야 합니다.
      서로 관계 없는 색을 넷 이상 섞으면 흐리멍덩해집니다.
    · 흰색에 가까운 밝은 면을 머리나 몸통에 하나 크게 두세요.
      얼굴은 그 밝은 면 위에 올립니다. 어두운 면 위에 얼굴을 두지 마세요.
    · 행성 배경 색을 몸통 큰 면에 입히지 마세요. 배경과 같은 색이면 묻힙니다.
      행성 색은 액센트에만 씁니다.
    · 가슴·이마의 표식은 선택입니다. 어울리는 캐릭터(로봇 등)에만 넣고,
      넣는다면 하나만 둡니다. 모든 캐릭터의 공통 요소가 아닙니다.

    ■ 출력 형식
    · 1:1 정사각형, 캐릭터 한 명당 이미지 한 장.
    · 배경은 투명이 아니라 단색으로 꽉 채워 주세요. 색은 그 캐릭터에 전혀 쓰이지
      않았고 캐릭터와 대비가 가장 큰 색 하나를 생성 모델이 고릅니다 (누끼용).
      그라데이션·무늬·그림자·바닥선 없이 평면 단색. 쓴 배경색 hex를 기억해 둡니다.
      (2026-08-24 개정 — 마젠타 고정에서 자유색으로. 검사 도구에 --key=0x<hex>를
       반드시 함께 넘긴다. 안 넘기면 기본값 마젠타를 지우려다 멈춘다.)
    · 캐릭터가 정사각형 한가운데 오게 하고, 세로로 캔버스의 85~90%를 채웁니다.

    ■ 톤
    · 친근하고 귀엽습니다. 무섭거나 징그럽거나 기괴하면 안 됩니다.
    · 초등 3학년부터 중학생까지가 봅니다. 유아용으로 보이지 않으면서 다정해야 합니다.
    · 배경과 오브젝트는 배경으로 물러나 있고, 캐릭터가 주인공으로 딱 보여야 합니다.

    ■ 가장 중요한 것
    첨부한 기준 캐릭터와 **같은 사람이 같은 날 그린 형제**로 보여야 합니다.
    행성마다 개성은 다르되, 머리·몸 비율, 눈과 입 모양, 손발의 뭉툭함,
    선 없는 플랫한 그림체, 색을 다루는 방식이 똑같아야 합니다.

## 1차 결과 — 무엇이 통했고 무엇이 안 통했나

GPT에게 위 프롬프트로 3종(로봇 도시 · 마법의 밤 · 용암 사막)을 한 번에 그리게 했다.
**A(로봇 도시)만 통했다.** 사용자 판정은 「A는 그냥 쓴다, B·C는 못 쓸 수준」이었고,
A는 이미 `elem-3-1`에 적용해 확정했다.

### A가 통한 이유 — 이것이 이번 회차의 실질 기준이다

- **구조가 눈에 보인다.** 둥근 머리 · 좌우 귀 부품 · 가슴판이 있는 몸통 ·
  관절이 표시된 짧은 팔다리. 부위 경계가 하나하나 읽힌다.
- **색이 두 가지다.** 흰색 + 채도 높은 파랑, 여기에 네이비 관절과 아주 작은 금색.
  세지 않아도 몇 색인지 알 수 있는 수준.
- **얼굴이 큰 밝은 면 위에 있다.** 흰 머리 위 까만 콩알눈이라 멀리서도 얼굴이 먼저 읽힌다.
- **몸을 덮는 것이 없다.** 망토·로브·후드가 없어서 실루엣이 캐릭터로 읽힌다.
- **표식이 하나다.** 가슴의 육각 코어 + 머리 안테나. 그게 이 캐릭터의 이름표다.

### B(마법의 밤)가 실패한 이유

- **로브와 후드가 몸을 다 덮었다.** 팔이 작은 혹처럼 남고 몸통·다리 경계가 사라졌다.
  덩어리로 읽히고, 나중에 팔다리를 떼어 움직이게 만들 수도 없다.
- **얼굴이 연보라 면 위에 있었다.** 후드 안 연보라 얼굴이라 윤곽이 흐물흐물하다.
- 전체가 보라 한 계열이라 몸통이 배경과 같은 색이 됐다.

### C(용암 사막)가 실패한 이유

- **색이 네 가지로 흩어졌다.** 크림 몸통 + 갈색 벨트 + 청록 팔다리 + 주황 결정.
  서로 관계 없는 색이 모여 흐리멍덩해졌다.
- **머리가 헬멧 덩어리가 됐다.** 이마 밴드 때문에 잠수 헬멧처럼 읽힌다.
- 크림색 몸통은 모래 행성 아홉 종에서 전부 묻히는 색이다.

### 그래서 이렇게 간다 — 존재는 다르되, 그림체는 하나 (2026-08-24 v2 · Try 1 교훈 반영)

**전부 로봇으로 만들지 않는다.** 로봇은 초3-1이 로봇 행성이라 로봇인 것이고,
나머지는 각자 다른 존재다 — 파라오, 진저쿠키맨, 눈사람, 머구리 다이버….
「같은 탐험가가 장비만 바꾼다」는 접근은 폐기됐다.

**Try 1의 교훈 — 통일 장치를 얼굴에 걸면 안 된다.** 얼굴(콩알눈·ω입)과
비례(2등신)를 고정했더니 16종이 「같은 얼굴이 옷만 갈아입은」 코스튬 놀이가
됐고, 사용자 판정은 「디자인을 덜 했다」였다. 통일은 **그림체 층위**(컬러링·
면 처리·조형 언어)에서 하고, 정체성은 **존재 층위**에서 푼다. 메이플스토리의
슬라임과 발록은 얼굴도 비례도 다르지만 같은 붓이 그린 것처럼 보인다 — 그 원리다.

**스타일 앵커는 기준 로봇 한 장이 아니라 그 행성의 오브젝트 6종이다.**
32화면이 이미 같은 그림체로 통일돼 있다. 「그 행성 오브젝트들과 같은 손이 그린
캐릭터」가 기준이고, 초3-1 로봇은 컬러링·조형 언어의 참고일 뿐 얼굴 템플릿이 아니다.

| 그림체 — 16종 전부 동일 (기능이 이유인 규칙) | 행성별로 자유 |
| --- | --- |
| 외곽선 없는 플랫 벡터 · 면 분할 명암 · 둥근 조형 · 질감/3D 금지 | 존재 자체 · 얼굴 생김새 · 등신 비례 |
| 주조 2~3색 + 소량 액센트 (「2색 엄수」는 완화) | 모자·뿔·날개 등 외형 특징 · 소품 |
| 얼굴은 밝은 면 위 · 행성 배경색을 몸의 큰 면에 안 씀 | 액센트 색 (그 행성 보색 쪽) |
| A자 정면 자세 · 부위 경계가 읽히게 (idle 리깅용) | 다리 없는 존재 허용 (눈사람 — idle은 통통 튀기) |
| 사람형이어도 **사람으로 표현하지 않음** (피부톤 인간 금지) | 가슴 표식 (선택) |

단, 자유라고 해도: 징그럽거나 기괴한 창작 크리처 금지. **누구나 아는 원형의
약간의 변형안**이 목표다. 원숭이는 쓰지 않는다 (나중에 서유기 테마 예정).

## 색 원칙

- **행성 색을 몸통 큰 면에 입히지 않는다.** 행성 색은 액센트·표식에만.
- **흰색에 가까운 밝은 면을 반드시 하나 크게 둔다.** 얼굴은 그 면 위에 올린다.
- **모래·베이지 행성이 16종 중 9종이다.** 몸통을 크림·베이지로 하면 절반이 묻힌다.
- 가장 어두운 행성(중1-2 마법의 밤, 중3-2 심해)에서도 이 밝은 면이 캐릭터를 살린다.
  가장 밝은 행성(중2-2 설원)에서는 관절·신발의 어두운 면이 일한다.

## 행성 16종 — 테마와 캐릭터 컨셉 (2026-08-24 사용자 확정)

수학은 그 행성 표면, 과학은 같은 행성을 우주선 안에서 본 화면이다.
캐릭터 컨셉과 액센트 색은 사용자와 확정한 설계도다. 임의로 바꾸지 않는다.
(액센트 색은 실제 배경에 올린 검사에서 조정될 수 있다.)

「사람형 대원」은 전부 초3-1 로봇처럼 **사람으로 표현하지 않는** 존재다
(피부톤 인간 금지). 나머지는 누구나 아는 원형의 약간의 변형안.

| 학기키 | 행성 테마 | 존재 (v2 확정) | 외형 특징 · 액센트 색안 |
| --- | --- | --- | --- |
| `elem-3-1` | 파란 미래도시 · 로봇 행성 | 로봇 | 안테나 + 육각 코어 · 파랑 — **완료 · 기준안** |
| `elem-3-2` | 정글 탐사 행성 | 정글 탐사 대원 (비인간) | 사파리 모자·고글 · 코랄 주황 |
| `elem-4-1` | 팩토리 행성 | 정비 대원 (비인간) | 헤드램프 · 멜빵(팔다리 경계 유지) · 안전 노랑 |
| `elem-4-2` | 스마트팜 행성 | 농부 대원 (비인간) | 밀짚모자 느낌 챙 + 새싹 · 토마토 레드 |
| `elem-5-1` | 이집트 나일강 유적 행성 | 파라오 느낌의 존재 | 네메스 두건 — 얼굴은 밝은 면 위 · 골드+블루 |
| `elem-5-2` | 그리스 로마 신화 행성 | 아테나 느낌의 존재 | 월계관 또는 작은 투구(눈 안 가리게) · 올빼미 소품 · 골드 |
| `elem-6-1` | 레고 조립 블럭 행성 | 블록맨 (몸이 브릭) | 머리 위 블록 스터드 · 블록 레드 |
| `elem-6-2` | 디저트 · 파티시에 행성 | **진저쿠키맨** (사용자 제시) | 갈색 쿠키 몸 + 아이싱 라인 · 캔디 단추 액센트 |
| `mid-1-1` | 해적 행성 | 꼬마 해적 선장 (비인간) | 작은 삼각모 · 닻 소품 가능 · 해적 레드 |
| `mid-1-2` | 마법 행성 (**가장 어두움**) | 꼬마 마법사 (비인간) | 고깔모자만 — **로브 금지** · 작은 지팡이 · 골드 |
| `mid-2-1` | 볼케이노 행성 (난색 극단) | 용암 꼬마 (골렘 느낌) | 작은 불꽃·결정 뿔 · 몸통 흰+차콜 · 용암 주황 |
| `mid-2-2` | 아이스 행성 (**가장 밝음**) | **눈사람** (사용자 제시) | 다리 없어도 됨(idle은 통통 튀기) · 아이스 블루 |
| `mid-3-1` | 음악 · 리듬 행성 | 락커 또는 DJ (비인간) | 기타 또는 헤드폰·턴테이블 · 마젠타 또는 노랑 |
| `mid-3-2` | 아틀란티스 · 심해 행성 | **머구리 딥다이버** | 둥근 헬멧 창 안에 얼굴(예외 허용) · 사용자 기존 작업물 느낌 참고 · 청록 발광 |
| `high-1-1` | 공룡 행성 | 아기 공룡 | 등의 세모 골판 · 작은 꼬리 · 얼굴·배는 밝은 면 · 그린 계열 |
| `high-1-2` | 님프 · 요정 행성 | 꽃 요정 | 잎·꽃 머리 장식 · 등 뒤 작은 날개(분리 가능한 별도 부위) · 연두 또는 핑크 |

`mid-3-1`(옛 록스타)과 `mid-3-2`(옛 잠수부)에는 옛 3등신 캐릭터가 들어 있다 — 교체 대상이다.
나머지 13학기는 말풍선만 서 있다.

## 역할 분담 — 누가 무엇을 하는가

그림 생성은 **Codex**가 한다(사용자의 ChatGPT 구독으로 붙어 있는 이미지 생성).
클로드 방은 생성을 하지 않는다. 클로드 방이 하는 일은 둘뿐이다.

| 클로드 방 | Codex |
| --- | --- |
| ① 그 행성의 자료를 뽑는다 (`planet-ref.py`) | 그림을 생성한다 |
| ② Codex에 붙여넣을 **프롬프트를 만들어 준다** | 누끼 따고 배경에 올려 스스로 판정한다 |
| ③ 돌아온 이미지를 **webp로 변환해 설치한다** | 별로면 수정 1회까지 다시 한다 |
| ④ 실제 화면에서 확인하고 보고한다 | 그래도 안 되면 포기하고 다음 지침을 적어 온다 |

즉 한 행성의 흐름은 이렇다.

    클로드: 자료 뽑기 → 프롬프트 출력
      ↓ (사용자가 Codex에 붙여넣는다)
    Codex: 생성 → 누끼 → 배경 합성 → 자기 판정 → (필요하면) 수정 1회 → PNG 저장
      ↓ (사용자가 결과를 클로드 방에 알린다)
    클로드: webp 변환 → 설치 → 화면 확인 → 보고

**클로드 방이 이미지를 직접 만들려 들지 말 것.** `gen-image` 스킬이 있긴 하지만
이 회차의 방식은 「프롬프트를 넘기고 결과를 받는다」로 정해져 있다.
사용자가 명시적으로 바꾸자고 할 때만 다르게 한다.

## ① 자료 뽑기 (클로드 방)

    python3 tools/planet-ref.py <학기키> ~/Desktop/RM-캐릭터/<학기키>

수학 화면 · 과학 화면 · 오브젝트 6종이 나온다. 경로를 프롬프트에 그대로 적어 준다.

## ② Codex 진행 방식 — 온보딩 1회 + 같은 세션에서 배치 (2026-08-24 개정)

행성마다 새 대화를 열지 않는다. 매번 독립 시행이 되면 세계관 맥락이 누적되지
않아 회차마다 방향이 조금씩 표류한다 — 「첫 작업은 되는데 전개가 안 되는」 원인.

1. **킥오프 1회.** 클로드 방이 만든 킥오프 프롬프트를 사용자가 Codex에 붙여넣는다.
   32화면 시트 · 기준 캐릭터 · 행성 16종 설계도 · 그림 조건이 전부 들어 있고,
   Codex는 **그리기 전에 이해한 바와 방향을 먼저 답하고 멈춘다.**
   방향이 어긋나면 그림 낭비 없이 여기서 교정한다.
2. **같은 세션에서 배치 진행.** 한 배치 3~4행성. 첫 배치는 극단 3종 —
   `mid-1-2`(가장 어두움·마법) · `mid-2-1`(난색 극단·볼케이노) · `mid-2-2`(가장 밝음·아이스).
   1차에서 죽었던 바로 그 조건들이라, 여기가 뚫리면 나머지 12종은 더 쉽다.
3. **배치가 끝나면 라인업 검사.** 클로드 방이 확정본 전원을 나란히 세운 라인업
   시트를 합성해 보여준다. 판정 질문은 「A와 닮았나」가 아니라 **「이 줄이 한
   가족으로 보이나」**다 — 1:1 비교만 하면 조금씩 표류한 게 안 보인다.

온보딩 자료 (클로드 방이 만든다 — `planet-ref.py`의 `screen()`을 재사용해
4×4 타일로 합성한다. 브라우저 캡처가 아니라 iframe 로딩 문제가 없다):

    ~/Desktop/RM-캐릭터/0_온보딩/1_수학_16행성.png
    ~/Desktop/RM-캐릭터/0_온보딩/2_과학_16행성.png

킥오프 프롬프트에 반드시 들어가야 하는 것 — 빼면 1차 실패가 재발한다:
「확정된 그림 조건」 본문 전체(특히 **구조**와 **색**), 행성 16종 설계도 표,
아래의 행성별 자체 검사 절차, 수정 1회 제한, 저장 규칙.

### 행성별 자체 검사 — 캐릭터를 뽑은 직후 Codex가 스스로 한다
    캐릭터를 뽑았으면 여기서 멈추지 말고 아래를 직접 해 봐.
    작업 폴더는 ~/Documents/Portfolio/projects/RM 이다.

        python3 tools/char-on-bg.py <생성한파일.png> <학기키> <출력폴더> --key=0x<배경색hex>

    (--key는 생성할 때 채운 배경색이다. 빼면 기본값 마젠타를 지우려다 멈춘다.)
    누끼를 따서 수학·과학 홈 배경에 실제 좌표로 올린 검사 이미지와
    대비 수치가 나온다. 그 이미지를 눈으로 보고 다섯 가지를 판정해.

    ① 그 화면에서 가장 먼저 눈이 가는 것이 캐릭터인가 (오브젝트가 이기면 실패)
    ② 부위 경계가 하나하나 읽히는가 — 머리·몸통·팔(·다리) (뭉개지면 실패.
       눈사람처럼 다리 없는 존재는 몸 덩어리끼리의 경계로 판정)
    ③ 색이 정돈돼 보이는가 (관계 없는 색이 넷 이상 흩어지면 실패)
    ④ 얼굴이 밝은 면 위에 있고 멀리서도 먼저 읽히는가
    ⑤ 기준 캐릭터·오브젝트들과 같은 붓이 그린 것으로 보이는가
       (얼굴·비례가 같은가가 아니다 — 그림체로 판정한다)

    숫자는 보조다. 자체 대비 8 이상이면 합격권(기준안이 11),
    5 미만이면 구조가 뭉갠 것이다. 몸 대비는 참고만 한다 —
    기준안도 1.14라서 그 숫자로 자르면 합격안까지 떨어진다.

    ■ 별로면 딱 한 번만 수정
    무엇이 문제였는지 한 줄로 정하고, 그 한 가지만 바꿔 다시 생성해.
    다시 검사해서 괜찮으면 그걸로 저장한다.
    두 번째도 별로면 **포기하고 멈춰.** 세 번째는 만들지 마.
    대신 「다음에 다시 시도하면 무엇을 어떻게 바꿀지」를 답에 적어 줘.

    ■ 저장
    통과한 원본 PNG(배경 단색 그대로)와 누끼 PNG를 여기에 둬.
      ~/Documents/_source/RM/generated/img/character/
      파일명: planet-<학기키>-<테마슬러그>-v1.png  (누끼는 뒤에 -cut)
    이 폴더는 깃이 없다. 기존 파일을 덮어쓰지 마.

    ■ 답에 적어 줄 것
    몇 번째 시도로 통과했는지, 대비 수치, 다섯 판정의 결과,
    (실패했으면) 다음에 무엇을 바꿀지.

## ③ 설치 (클로드 방)

Codex가 저장한 누끼 PNG를 받아서:

    cwebp -quiet -q 92 -alpha_q 100 <누끼.png> -o /tmp/ch.webp
    cp /tmp/ch.webp assets/img/home/math-<학기키>/character.webp
    cp /tmp/ch.webp assets/img/home/sci-<학기키>/character.webp

그리고 **셋을 반드시 함께 한다.**

1. `nav/home.html`의 `HAS_CHAR`에 `math-<학기키>`·`sci-<학기키>` 두 키 추가
2. `nav/home.html`의 `CHAR_V` 숫자 올리기
3. 서버(rm-static 8020)에서 수학·과학 두 화면 실제 확인

누끼가 Codex에서 안 왔으면 클로드 방에서 딴다 (--key는 그 원본의 배경색).

    python3 tools/char-on-bg.py <원본.png> <학기키> <출력폴더> --key=0x<배경색hex>

## ④ 보고 (클로드 방)

실제 화면 스크린샷 두 장(수학·과학)과 행성별 표.

## 판정 기준 — 눈이 주(主)이고 숫자는 보조다

**합격선은 A행성이다.** A를 재 보면 이렇게 나온다.

    화면              몸 대비   자체 대비
    수학 · 행성 표면     1.14     11.09
    과학 · 우주선 안     1.20     11.09

**몸 대비 1.14는 게이트로 쓸 수 없다.** 그 숫자로 자르면 사용자가 합격시킨 A까지
떨어진다. 실제 화면에서 미완료 오브젝트는 홀로그램(무채색)이라 컬러 캐릭터가
저절로 튄다 — `char-on-bg.py`는 오브젝트를 전부 실물로 깔기 때문에 최악 조건이다.

그래서 숫자는 이렇게만 쓴다.

- **자체 대비 ≥ 8** — 합격권. A가 11이다. 5 미만이면 구조가 뭉갠 것이니 다시 본다.
- **몸 대비** — 참고만. 1.0 근처면(배경과 밝기가 똑같으면) 밝은 면을 더 밝게 한다.

**판정은 검사 이미지를 눈으로 보고 한다. 다섯 가지를 본다.**

1. 그 화면에서 **가장 먼저 눈이 가는 것이 캐릭터인가.** 오브젝트가 이기면 실패.
2. **부위가 하나하나 보이는가** — 머리 · 몸통 · 팔 · 다리. 덩어리로 읽히면 실패.
   (B가 여기서 죽었다)
3. **몇 색인지 세지 않고 알 수 있는가.** 네 색 이상 흩어지면 실패. (C가 여기서 죽었다)
4. **얼굴이 밝은 면 위에 있고 멀리서도 먼저 읽히는가.**
5. **A·오브젝트들과 같은 붓이 그린 것으로 보이는가.** (v2 개정 — 눈·입·비율이
   같은가로 판정하지 않는다. 그렇게 판정하면 Try 1의 코스튬 놀이로 돌아간다.)

2번과 3번이 이번 회차의 실제 사망 원인이다. 대비보다 먼저 본다.

## 하지 말 것

- **클로드 방에서 이미지를 직접 생성하는 것.** 그림은 Codex가 만든다.
  클로드 방은 자료 뽑기 · 프롬프트 만들기 · webp 변환·설치 · 화면 확인만 한다.
- 프롬프트의 그림체·비율·얼굴 조건을 바꾸는 것. 기준안이 그걸로 나왔다.
- 프롬프트에서 **「구조」와 「색」 항목을 빼는 것.** 1차 실패의 실제 원인이다.
- 투명 배경을 요청하는 것. 생성 모델이 잘 못 만든다. **단색 fill이 확정 방식**이다
  (색은 캐릭터와 대비가 가장 큰 색을 모델이 고르고, 검사에 `--key=0x<hex>`로 넘긴다).
- 한 행성에 세 번 이상 생성하는 것. 두 번에 안 나오면 다음 회차로 넘긴다.
- 오브젝트 좌표(`LAYOUT`)를 만지는 것. 과목당 한 벌 고정이다.
- 원본 PNG를 프로젝트 안에 넣는 것. `_source`에 두고 webp만 프로젝트에.
- 검사용으로 만든 임시 폴더를 `tools/`에 남기는 것. 도구 2개만 남는다.

## 알아 둘 것

- **캐릭터를 교체하면 `nav/home.html`의 `CHAR_V` 숫자를 올린다.** 파일명이 그대로여서
  브라우저가 옛 그림을 계속 쓰는 문제가 실제로 났다. 배경·오브젝트는 안 바뀌므로
  캐릭터 URL에만 붙여 뒀다.
- **캐릭터 칸의 폭은 과목마다 다르다** (2026-08-25 · `LAYOUT`의 `hero`).
  수학 31.25% · 과학 25%. 그림은 한 장을 두 화면이 공유하고 **칸 크기만 다르다.**

  | | 중앙 오브젝트 폭 | 캐릭터 칸 폭 | 캐릭터/중앙 |
  | --- | --- | --- | --- |
  | 수학 (실외) | 49.4% | 31.25% | 0.63 |
  | 과학 (실내) | 32.1% | 25% | 0.78 |

  과학은 우주선 안이라 오브젝트가 전부 작다. 같은 31.25%를 쓰면 캐릭터가 중앙
  구조물과 같은 크기(0.97배)가 돼 혼자 커 보인다 — 실제로 그렇게 났다.
  20% 이하로 줄이면 이번엔 홀로그램 오브젝트와 비슷해져 주인공으로 안 읽힌다.
  **`tools/planet-ref.py`·`char-on-bg.py`의 `HERO_W`도 같은 값이다** — 한쪽만
  고치면 검사 이미지가 실제 화면과 다른 크기로 나온다. 셋을 함께 고친다.
  「캐릭터가 작아/커 보인다」는 대개 그림 문제가 아니라 이 칸 크기 문제다.
- 캐릭터 위에는 **CTA 버튼과 토스트만** 올라온다(z-index 40·50).
  오브젝트도 명판도 캐릭터를 가리지 않는다.
- `nav/home.html`은 캐릭터에 이미 드롭섀도를 넣는다
  (`filter:drop-shadow(0 16px 18px rgba(15,35,80,.4))`). 대비가 아슬아슬한 학기는
  **그림을 다시 뽑는 대신 이 그림자나 외곽 글로우를 세게 하는 것이 훨씬 싸다.**
  다시 뽑기 전에 이 선택지를 먼저 검토할 것.
- 나중에 몸·팔·다리를 분리해 idle 모션을 넣을 계획이 있다. Rive도 후보다.
  다만 **이번 회차에서는 하지 않는다** — 기본 캐릭터가 어떻게 나오는지가 먼저다.

## 보고 방식

- 사용자는 UI/UX 디자이너다. 코드를 읽지 않는다. 한국어로, 결론 먼저.
- 보고는 **검사 이미지로** 한다. 파일명 나열이 아니다.
- 행성별로 표를 하나 만든다: 학기 · 몸 대비 · 자체 대비 · 판정 · 몇 번째 시도 · 결과.
- 포기한 학기는 **다음에 무엇을 바꿀지**를 반드시 적는다.
- 임의로 정한 것은 마무리에 목록으로 모아 보고한다.
